// Conexão com o banco de dados. Responsável: Amanda/Samuel
// Ex.: pool do PostgreSQL (pg) ou conexão do Mongoose, conforme decisão do grupo.

// import { Pool } from "pg";
// export const pool = new Pool({ connectionString: process.env.DATABASE_URL });

export {};
